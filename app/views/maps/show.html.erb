<!--顧客のマイページと連動-->

<h1>Map Details</h1>

<p><strong>Lat:</strong> <%= @map.lat %></p>
<p><strong>Lng:</strong> <%= @map.lng %></p>
<p><strong>title:</strong> <%= @map.title %></p>
<p><strong>Text:</strong> <%= @map.text %></p>
<p>
  <strong>Tags:</strong>
  <% @map.tags.each do |tag| %>
  <ul style="list-style:none;">
    <li style="display:inline;padding:8px 16px;">#<%= link_to tag.name, root_path %></li>
  </ul>
  <% end %>
</p>

<%= link_to 'Edit', edit_map_path(@map) %>
<%= link_to 'Back', maps_path %>


<a class="twitter-share-button"
  href="https://twitter.com/intent/tweet?text=<%= @map.title %>" target="_blank" rel="noopener">
Tweet</a>
